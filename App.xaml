<Application x:Class="Device_Library_WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:Device_Library_WPF"
             
             StartupUri="Views/MainWindow.xaml">
    <Application.Resources>

        <!--  Цвета  -->
        <Color x:Key="C_Bg">#FF2B2B2B</Color>
        <Color x:Key="C_Surface">#FF303030</Color>
        <Color x:Key="C_Card">#FF2F2F2F</Color>
        <Color x:Key="C_Card2">#FF262626</Color>
        <Color x:Key="C_Text">#FFF2F2F2</Color>
        <Color x:Key="C_Text2">#FFBDBDBD</Color>
        <Color x:Key="C_Border">#3FFFFFFF</Color>
        <Color x:Key="C_Accent">#FFFFFFFF</Color>

        <SolidColorBrush x:Key="BgBrush" Color="{StaticResource C_Bg}" />
        <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource C_Surface}" />
        <SolidColorBrush x:Key="CardBrush" Color="{StaticResource C_Card}" />
        <SolidColorBrush x:Key="CardBrush2" Color="{StaticResource C_Card2}" />
        <SolidColorBrush x:Key="TextBrush" Color="{StaticResource C_Text}" />
        <SolidColorBrush x:Key="Text2Brush" Color="{StaticResource C_Text2}" />
        <SolidColorBrush x:Key="BorderBrushSoft" Color="{StaticResource C_Border}" />
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource C_Accent}" />

        <!--  Скругления  -->
        <CornerRadius x:Key="R_Outer">18</CornerRadius>
        <CornerRadius x:Key="R_Card">20</CornerRadius>
        <CornerRadius x:Key="R_Control">14</CornerRadius>
        <CornerRadius x:Key="R_Button">14</CornerRadius>

        <Thickness x:Key="P_Window">24</Thickness>
        <Thickness x:Key="P_Card">18</Thickness>
        <Thickness x:Key="P_Control">12,8</Thickness>

        <DropShadowEffect x:Key="ShadowSoft" BlurRadius="24" ShadowDepth="0" Opacity="0.35" />

        <!--  Стильи текста  -->
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="TextOptions.TextFormattingMode" Value="Display"/>
            <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
        </Style>

        <Style x:Key="TitleText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Cascadia Code"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="HeaderText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Cascadia Code"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
        </Style>

        <Style x:Key="MutedText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Cascadia Code"/>
            <Setter Property="Foreground" Value="{StaticResource Text2Brush}"/>
        </Style>

        <!--  Общие контейнеры -->
        <Style x:Key="CardContainer" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="CornerRadius" Value="{StaticResource R_Outer}"/>
            <Setter Property="Padding" Value="20"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="PillHeader" TargetType="Border">
            <Setter Property="Background" Value="#18FFFFFF"/>
            <Setter Property="CornerRadius" Value="{StaticResource R_Button}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="14,8"/>
        </Style>

        <Style x:Key="InfoLine" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Cascadia Code"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <!-- Textbox стиль-->
        <Style TargetType="TextBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource BgBrush}"/>
        </Style>

        <!-- Темная тема для ComboBox -->
        <Style x:Key="DarkComboBox" TargetType="ComboBox">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource CardBrush2}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid>
                            <Border x:Name="Bd"
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">
                                <DockPanel>
                                    <!-- стрелка -->
                                    <ToggleButton DockPanel.Dock="Right"
                                  Focusable="False"
                                  IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}"
                                  Background="Transparent"
                                  BorderThickness="0"
                                  Width="34">
                                        <Path Data="M 0 0 L 4 4 L 8 0 Z"
                                    Fill="{StaticResource TextBrush}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"/>
                                    </ToggleButton>

                                    <!-- выбранный элемент -->
                                    <ContentPresenter Margin="{TemplateBinding Padding}"
                                VerticalAlignment="Center"
                                HorizontalAlignment="Left"
                                Content="{TemplateBinding SelectionBoxItem}"
                                ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}"
                                RecognizesAccessKey="True"/>
                                </DockPanel>
                            </Border>

                            <!-- выпадашка -->
                            <Popup IsOpen="{TemplateBinding IsDropDownOpen}"
                            Placement="Bottom"
                            AllowsTransparency="True"
                            Focusable="False"
                            PopupAnimation="Slide">
                                <Border Background="{StaticResource CardBrush}"
                BorderBrush="{StaticResource BorderBrushSoft}"
                BorderThickness="1"
                Margin="0,6,0,0">
                                    <ScrollViewer>
                                        <ItemsPresenter />
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Темная тема для ComboBoxItem -->
        <Style TargetType="ComboBoxItem">
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBoxItem">
                        <Border x:Name="B" Background="{TemplateBinding Background}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="B" Property="Background" Value="#20FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="B" Property="Background" Value="#30FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  Кнопки  -->
        <Style x:Key="PillButton" TargetType="Button">
            <Setter Property="FontFamily" Value="Cascadia Code"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="#2FFFFFFF"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="18,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource R_Button}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#55FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  Скругленный TextBox  -->
        <ControlTemplate x:Key="DarkTextBox" TargetType="{x:Type ComboBox}">
            <Grid/>
        </ControlTemplate>
        <Style x:Key="RoundedTextBox" TargetType="TextBox">
            <Setter Property="FontFamily" Value="Cascadia Code"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Background" Value="{StaticResource BgBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrushSoft}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="{StaticResource P_Control}"/>
            <Setter Property="CaretBrush" Value="{StaticResource TextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{StaticResource R_Control}">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"
                                          VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter Property="BorderBrush" Value="#66FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#55FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  скругленный ScrollBar  -->
        <Style x:Key="RoundedScrollBar" TargetType="ScrollBar">
            <Setter Property="Width" Value="10"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ScrollBar">
                        <Grid Background="Transparent" Margin="5">
                            <Track x:Name="PART_Track" IsDirectionReversed="True">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineUpCommand"
                                                  Opacity="0" IsTabStop="False"/>
                                </Track.DecreaseRepeatButton>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Command="ScrollBar.LineDownCommand"
                                                  Opacity="0" IsTabStop="False"/>
                                </Track.IncreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb>
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Background="#66FFFFFF"
                                                        CornerRadius="5"/>
                                                <ControlTemplate.Triggers>
                                                    <Trigger Property="IsMouseOver" Value="True">
                                                        <Setter Property="Opacity" Value="0.9"/>
                                                    </Trigger>
                                                    <Trigger Property="IsDragging" Value="True">
                                                        <Setter Property="Opacity" Value="1"/>
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!--  ListBox шаблон  -->
        <Style x:Key="DevicesListBoxStyle" TargetType="ListBox">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBox">
                        <Border Background="Transparent">
                            <ScrollViewer Focusable="False">
                                <ScrollViewer.Resources>
                                    <Style TargetType="ScrollBar" BasedOn="{StaticResource RoundedScrollBar}"/>
                                </ScrollViewer.Resources>
                                <ItemsPresenter/>
                            </ScrollViewer>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ListBoxItem шаблон стиля -->
        <Style x:Key="DeviceListBoxItemStyle" TargetType="ListBoxItem">
            <Setter Property="FontFamily" Value="Cascadia Code"/>
            <Setter Property="Padding" Value="0"/>
            <Setter Property="Margin" Value="0,0,0,16"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="root"
                                Background="Transparent"
                                CornerRadius="{StaticResource R_Card}">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#12FFFFFF"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="root" Property="Background" Value="#0EFFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>


        <!--  Device item шаблон  -->
        <DataTemplate x:Key="DeviceItemTemplate">

            <Border Background="{StaticResource CardBrush}"
                    CornerRadius="{StaticResource R_Card}"
                    Padding="{StaticResource P_Card}"
                    BorderBrush="{StaticResource BorderBrushSoft}"
                    BorderThickness="1">

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="260"/>
                        <ColumnDefinition Width="24"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Image block -->
                    <Border Grid.Column="0"
                        Background="#1AFFFFFF"
                        CornerRadius="{StaticResource R_Card}"
                        BorderBrush="{StaticResource BorderBrushSoft}"
                        BorderThickness="1"
                        Margin="5,0,5,0"
                        Height="220">
                        <Grid>
                            <Image Source="{Binding DeviceImage}"
                               Stretch="UniformToFill"
                               ClipToBounds="True"/>

                            <!-- текст если DeviceImage == null -->
                            <TextBlock Text="DeviceImage"
                               Foreground="{StaticResource TextBrush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"
                               Opacity="0.85">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding DeviceImage}" Value="{x:Null}">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>

                    <!-- Text block -->
                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,6,0,0">
                        <TextBlock Style="{StaticResource TitleText}">
                            <Run Text="{Binding Manufacturer}"/>
                            <Run Text="{Binding Model}"/>
                        </TextBlock>

                        <StackPanel Margin="0,18,0,0">
                            <TextBlock>
                                <Run Text="Memory:"/>
                                <Run Text="{Binding MemoryText, Mode=OneWay}"/>
                            </TextBlock>
                            <TextBlock Margin="0,6,0,0">
                                <Run Text="CPU:"/>
                                <Run Text="{Binding Cpu}"/>
                            </TextBlock>
                            <TextBlock Margin="0,6,0,0">
                                <Run Text="Screen:"/>
                                <Run Text="{Binding ScreenType}"/>
                                <Run Text="{Binding Resolution, StringFormat={}{0}p}"/>
                                <Run Text="{Binding RefreshRate, StringFormat={}{0}Hz}"/>
                            </TextBlock>
                        </StackPanel>

                        <!-- Кнопки -->
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Right"
                                    Margin="0,50,0,0">
                            <Button Style="{StaticResource PillButton}"
                                    Content="Info"
                                    Width="50"
                                    Height="30"
                                    Margin="0,0,12,0"
                                    Command="{Binding DataContext.InfoCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                    CommandParameter="{Binding}" />
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>

    </Application.Resources>
</Application>
